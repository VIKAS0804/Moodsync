# Copy to app/.env — Expo inlines EXPO_PUBLIC_* vars at build time.

# Backend URL. Leave unset in development: the app derives your machine's LAN IP
# from the Expo host, since a physical device can't reach "localhost".
# EXPO_PUBLIC_API_URL=http://192.168.1.20:8000
